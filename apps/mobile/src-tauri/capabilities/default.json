{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "default",
  "description": "The shell's own permissions. Everything else rides the socket.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "dialog:default", "notification:default"]
}
